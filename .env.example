# Environment Variables Template
# Personal Budget Application Environment Variables
# Copy this file to .env and fill in your actual values
# For local development, you can also create .env.local to override these values

# MongoDB Database Connection
# For local development (requires local MongoDB):
# DATABASE_URL="mongodb://localhost:27017/personalbudget"
# For production (MongoDB Atlas):
DATABASE_URL="mongodb+srv://username:password@cluster.mongodb.net/database?retryWrites=true&w=majority"

# NextAuth Configuration
# Generate a random secret: openssl rand -base64 32
# Keep this secret secure and never commit actual values to version control
NEXTAUTH_SECRET="your-nextauth-secret-here-replace-with-actual-secret"

# Application URL
# For local development:
# NEXTAUTH_URL="http://localhost:3000"
# For production:
NEXTAUTH_URL="https://your-domain.vercel.app"

# Environment (automatically set by hosting platforms)
NODE_ENV="development"